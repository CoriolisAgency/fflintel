# Pages origin. Turn this workflow off after fflintel.com is on Vercel
# (see docs/dns-cutover.md). Two production origins will drift.
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # Custom domain fflintel.com (root). For github.io project path only, use:
      #   ASTRO_BASE: /fflintel/
      #   ASTRO_SITE: https://coriolisagency.github.io
      - run: npm run build
        env:
          ASTRO_BASE: /
          ASTRO_SITE: https://fflintel.com
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
